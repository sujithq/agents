# Python dependencies for the pptx-to-sora-video agent skills
#
# Install with:  pip install -r requirements.txt

# PPTX parsing and note extraction
python-pptx>=0.6.23

# Image handling (fallback slide renderer, thumbnail generation)
Pillow>=10.0.0

# PDF → PNG rendering (used with LibreOffice export pipeline)
pymupdf>=1.24.0

# HTTP client for Sora 2 API calls
requests>=2.31.0
